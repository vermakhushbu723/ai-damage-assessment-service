{
    "name": "ai-damage-assessment-server",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Main AI ILA backend (API, database, cost engine, cause-check, Llama narration). YOLO inference lives in ../yolo-service (Python, unavoidable) -- see ../docs/ARCHITECTURE.md Section 8.",
    "engines": {
        "node": ">=22.5.0"
    },
    "scripts": {
        "start": "node src/server.js",
        "dev": "node --watch src/server.js"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "multer": "^2.0.1",
        "zod": "^3.23.8"
    }
}
